# Datart 应用库走 MySQL（与 charging_bigdata 业务库分离）
# Docker Compose 叠加启动时挂载为本文件 → /datart/config/datart.conf

datasource.ip=mysql
datasource.port=3306
datasource.database=datart
datasource.username=root
datasource.password=charging123

server.port=8080
server.address=0.0.0.0

datart.address=http://127.0.0.1:8088
datart.send-mail=false
